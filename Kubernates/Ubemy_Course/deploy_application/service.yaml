apiVersion: v1
kind: Service
metadata:
  labels:
    app: deploy-application
  name: srv-deploy-application
spec:
  type: NodePort

  selector:
    app: deploy-application

  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
    nodePort: 30080
  
  

