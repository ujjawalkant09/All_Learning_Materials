apiVersion: v1
kind: Service
metadata:
  labels:
    app: hpa-vpa
  name: srv-hpa-vpa
spec:
  type: NodePort

  selector:
    app: hpa-vpa-deploy-application

  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
    nodePort: 30080
  
  

